<div id ="navbar">
	<div class = padded_text>
	<%= link_to "My Loans", loans_path %></div>
	<div class = "padded_text">
	 Search</div>
	 <div class = "padded_text">
	 <%= link_to "Home", root_path %>  </div>
	 	<div class = padded_text>
	<%= link_to "My Items", items_path({user_id: current_user.id}) %></div>
<div id ="pending_items">
	 <% pending_count = current_user.pending_loan_count %>
	 <% if pending_count > 0 %>

	 <%= link_to "New loan requests (#{pending_count})" , loans_path({category:"pending"}) %>
	 <% end %>
	</div>
</div>